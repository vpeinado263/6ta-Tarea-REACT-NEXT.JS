/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['http2.mlstatic.com', 'www.arcor.com', 'encrypted-tbn0.gstatic.com'],
  },
};

export default nextConfig;
